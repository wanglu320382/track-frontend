# 生产构建（vite build）会加载此文件，VITE_* 会打入 dist
# 前端静态资源根路径（须与 Nginx/网关挂载路径一致，末尾加 /）
VITE_APP_BASE=/track/
# AES 加密配置（与后端保持一致）
VITE_TRACK_STAT_AES_KEY=trackbackendkey1
VITE_TRACK_STAT_AES_IV=trackbackendiv12
